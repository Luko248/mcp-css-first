# Smithery configuration file: https://smithery.ai/docs/build/project-config

startCommand:
  type: stdio
  commandFunction:
    # Use npx to run the published package
    |-
    (config) => ({ command: 'npx', args: ['-y', '@depthark/css-first@latest'] })
  configSchema:
    type: object
    properties: {}
  exampleConfig: {}
